import { StyleSheet } from "react-native";

export const CS = StyleSheet.create({
    // ============== EVENT AREA  ==============
  contactCard: {
    marginTop: 10
  },
  image: {
    alignSelf: 'center',
    height: 100,
    width: 100
  }
})
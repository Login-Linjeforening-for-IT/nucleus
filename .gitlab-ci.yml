build-job:
  tags:
    - macos
  stage: build
  script:
    - npm install
    - npx -y eas build --local \
      --non-interactive \
      --output-path ./build \
      --platform ios \
      --profile development \